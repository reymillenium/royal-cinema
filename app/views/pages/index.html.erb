<%
  contact = local_assigns.fetch(:contact, Contact.new)
  return_to = local_assigns.fetch(:return_to, request.fullpath)
  skills = @skills
  references = @references
  samples = @samples
%>

<!-- about -->
<%= render partial: "about" %>
<!-- //about -->

<!-- skills -->
<%= render partial: "skills", locals: { skills: skills } %>
<!-- //skills -->

<!-- services -->
<%= render partial: "services" %>
<!-- //services -->

<!-- interests -->
<%= render partial: "interests" %>
<!-- //interests -->

<!-- experience -->
<%= render partial: "experience" %>
<!-- //experience -->

<!-- education -->
<%= render partial: "education" %>
<!-- //education -->

<!-- projects -->
<%= render partial: "projects" %>
<!-- //projects -->

<!-- references -->
<%= render partial: "references", locals: { references: references } %>
<!-- //references -->

<!-- samples -->
<%= render partial: "samples_dynamic", locals: { samples: samples } %>
<!-- //samples -->

<!-- introduce -->
<%= render partial: "introduce" %>
<!-- //introduce -->

<!-- contact -->
<%= render partial: "contact", locals: {
  contact: contact,
  return_to: return_to
} %>

<!--calendly-->
<%= render partial: "calendly" %>
<!--//calendly-->